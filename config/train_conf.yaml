# @package _global_
defaults:
  - _self_
  - hydra: hydra
  - model: v0
  - data: billiard_ball
  - training: default
  - hpo: null

# Paths
log_dir: logs
checkpoint_dir: checkpoints
results_dir: results

# Reproducibility
seed: 42
deterministic: true

# Experiment type
experiment_type: training  # training, hpo, evaluation, comparison